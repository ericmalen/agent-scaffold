# Rules

Path-scoped instructions, read by both Claude Code and VS Code Copilot.
Each file declares its scope with `paths:` glob frontmatter and stays under
50 non-blank lines. One scope per file. Run the `ai-kit-check` skill to
audit this folder against the conventions.
