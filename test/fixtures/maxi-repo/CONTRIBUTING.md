# Contributing

- Branches: `feature/<ticket>-<slug>` (e.g. `feature/AP-123-asset-tags`).
- Commits: Conventional Commits (`feat(api): ...`, `fix(ui): ...`).
- Files are kebab-case; React components are PascalCase exports.
- Run `npm test` (all workspaces) before opening a PR.
