<!-- This file goes at {subdirectory}/AGENTS.md. No frontmatter, no glob —
     scope is by location. Copilot and other AGENTS.md-aware tools load it
     automatically when working anywhere in this subtree, on top of the root
     AGENTS.md. For Claude Code, also drop a sibling {subdirectory}/CLAUDE.md
     containing a single line — `@AGENTS.md` — so Claude picks up the same
     nested scope. Keep this file focused on what's *specific* to this subtree;
     universal rules belong in the root AGENTS.md. Keep it short — it loads on
     every interaction within this directory. -->

# TODO Subtree Name

TODO: one line — what this directory is and how its conventions differ from the
repo defaults.

## Architecture

TODO

## Conventions

TODO

## Do Not

TODO
